import core/string.maci
import core/sys.maci

; Hello World Program

; use the print macro from core/string.maci
print("Hello World!")

; memory macros

alloc(128)
realloc(R0, 256)
free(R0)

; exit macro

exit(0)